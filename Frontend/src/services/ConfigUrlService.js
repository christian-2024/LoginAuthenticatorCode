let API_BASE_URL = "";

if (window.location.hostname === "localhost") {
    API_BASE_URL = "https://localhost:61442";
} else {
    API_BASE_URL = "https://localhost:61442";
}

export default API_BASE_URL;